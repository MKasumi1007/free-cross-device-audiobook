# Setup Status

The project is in stage 0. There are no normal-user setup steps yet.

Codex will perform local installation, configuration and tests. It will pause only when GitHub/Firebase login, a verification code, a permission prompt, a public repository name, a rights confirmation, a voice preview, an irreversible deletion or a real iPhone test requires the user.

